{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    // Customize specific rules here
    "react-hooks/rules-of-hooks": "off",  // Disable hook rules if they're causing issues
    "@typescript-eslint/no-unused-vars": "warn", // Change unused vars from error to warning
    "no-console": "warn" // Example: set console statements to warnings instead of errors
  }
}
